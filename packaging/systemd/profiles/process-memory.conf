# Experimental opt-in: install with runtime-discovery.conf and enable both
# enable_runtime_discovery and enable_process_memory_scraping in agent TOML.
[Service]
AmbientCapabilities=CAP_SYS_PTRACE
CapabilityBoundingSet=CAP_SYS_PTRACE
ProcSubset=all
ProtectProc=ptraceable
